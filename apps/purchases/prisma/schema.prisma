// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl"]
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:wK62nr72edCyirELfrSY@containers-us-west-33.railway.app:7618/railway"
}

model Customer {
  id    String @id
  name  String
  email String

  purchases Purchase[]
}

model Product {
  id    String @id
  title String

  purchases Purchase[]
}

model Purchase {
  id         String @id
  customerId String
  productId  String

  customer Customer @relation(fields: [customerId], references: [id])
  product  Product  @relation(fields: [productId], references: [id])

  createdAt DateTime @default(now())
}
